1、构造函数和析构函数的作用。
答：
构造函数：构造一些函数用于新建对象进行初始化。
析构函数：用于撤销对象的一些特殊任务处理，可以是释放对象分配的内存空间。

2、Vector和数组的区别。并且回答Vector是如何创建兼容不同类型的变量。
答：
vector相对于数组支持动态扩容。
结构体类型中的vector就可以创建兼容不同类型的变量（要添加UnityEngine命名空间）

3、
答：
C继承A，B继承C，所以B也间接继承了A；同时B又直接继承A，A中数据的修改直接影响B；继承关系混乱。多继承的bug。
用虚基类(virtual base class)的方法，使得在继承间接共同基类时只保留一份成员。

4、
答：
（1）
classname(const classname &obj)
{
//构造函数的主题
}
（2）
回老家了，网不好下载个东西下载半天，目前用模拟器装好了Ubuntu的系统与Github的桌面应用。还有opencv没装与linux系统没装。
（3）矩阵可乘条件：如A*B，A的列数要等与B的行数。
